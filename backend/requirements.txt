# Clinical Dialogue Analyzer — Python Dependencies
# ─────────────────────────────────────────────────────────────
# Install:
#   pip install -r requirements.txt
#   pip install git+https://github.com/m-bain/whisperx.git
#
# Python 3.10+ required.
# Run server from this (backend/) directory:
#   uvicorn main:app --reload --host 0.0.0.0 --port 8000
# ─────────────────────────────────────────────────────────────

# ── API layer ─────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-multipart==0.0.9       # required for FastAPI file uploads

# ── Environment / config ───────────────────────────────────────
python-dotenv==1.0.1          # loads HF_TOKEN (and others) from .env

# ── NLP  (NLP_Neurolytics — exact versions) ───────────────────
transformers==4.40.2
torch==2.2.2
scipy==1.13.0
numpy<2

# ── Speech & Diarization (Phase 2) ────────────────────────────
# Install WhisperX separately after torch:
#   pip install git+https://github.com/m-bain/whisperx.git
#
# WhisperX sub-dependencies:
faster-whisper>=1.0.0
pyannote.audio>=3.1.1         # needs HuggingFace token (see .env.example)
ffmpeg-python>=0.2.0

# ── Dev / Testing ─────────────────────────────────────────────
httpx==0.27.0
pytest==8.2.0
