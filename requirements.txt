# Neurolytics — Python Dependencies
# ─────────────────────────────────────────────────────────────
# Exact versions sourced from NLP_Neurolytics/requirements.txt
# for full compatibility with the NLP team's module.
#
# Install:
#   pip install -r requirements.txt
#   pip install git+https://github.com/m-bain/whisperx.git
#
# Python 3.10+ required.
# ─────────────────────────────────────────────────────────────

# ── API layer ─────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-multipart==0.0.9       # required for FastAPI file uploads

# ── NLP  (NLP_Neurolytics — exact versions) ───────────────────
transformers==4.40.2
torch==2.2.2
scipy==1.13.0
numpy<2

# ── Speech & Diarization (Phase 2) ────────────────────────────
# Install WhisperX separately after torch:
#   pip install git+https://github.com/m-bain/whisperx.git
#
# WhisperX sub-dependencies:
faster-whisper>=1.0.0
pyannote.audio>=3.1.1         # needs HuggingFace token (see README)
ffmpeg-python>=0.2.0

# ── Dev / Testing ─────────────────────────────────────────────
httpx==0.27.0
pytest==8.2.0
python-dotenv==1.0.1
